package com.quane.glass.core.language

import org.junit.runner.RunWith
import org.scalatest.FunSuite
import org.scalatest.junit.JUnitRunner

@RunWith(classOf[JUnitRunner])
class TestStatementPanel
        extends FunSuite {

    // Test AND
    test("something") {
        assert(true)
    }

}
